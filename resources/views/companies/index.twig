{% extends 'templates/app.twig' %}

	{% block content %}
<div class="row">
	<div class="col-md-10">
		<div id="companylist">
			<div class="panel">
                <div class="panel-heading">
                    <div class="text-center">
                 
                        <div class="row">
                            <div class="col-sm-9">
                                <h3 class="pull-left">List of Companies:</h3>
                            </div>
                            
                        </div>
                  
                    </div>
                </div>
                
            <div class="panel-body">
                
                <div class="container">

<ol>
  {% for company in companies %}
    <li>
      <a href="{{ path_for('companies.view', {'id': company.id }) }}">{{ company.name }}</a> |
             {{ company.address }}
    </li>
  {% endfor %}
</ol>

<!-- {% for company in companies %}
<div class="row col-md-8">
  <div class="span12">
    <div class="row">
      <div class="span8">
        <h4><strong><a href="{{ path_for('companies.view', {'id': company.id }) }}">{{ company.title }}</a></strong>
        <small> <a class="btn" href="{{ path_for('users.view', {'id': company.user.id }) }}">
        {{  company.user.first_name }} {{  company.user.last_name }} </a> </small>
        </h4>
       
      </div>
    </div>
    <div class="row">
    
      <div class="span10">      
        <p>
        {{ company.body| slice(0, 150 ) ~ '...' }}
         </p>
      </div>
    </div>
   
  </div>
</div>
{% endfor %} -->

<hr>


</div>
<hr>
</div>


            </div>
            
            <div class="panel-footer">
            </div>
        </div>
        
       </div>
       {% if (auth.user.role_id <= 2) %}
       <div class="text-center">
            <a href="{{ path_for('companies.add') }}" class="btn btn-primary">Add New Company</a>
        </div> 
        {% endif %}
    </div>
</div>

</div>
	{% endblock %}
